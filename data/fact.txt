ELEM_IN
POP BX
PUSH 1
; Main info
; BX - arg to calc factorial
; Destroys BX
CALL fact:

ELEM_OUT
HLT

fact:
PUSH 2
PUSH BX
JA next:

PUSH BX

PUSH BX
PUSH 1
SUB
POP BX
CALL fact:
MULT

next:
RTN

;n * fact ( n - 1)

;fact(n + 1) * n;
;fact(n - 1)
